// ui/shared/index.ts

// FSD "shared" layer - barrel exports disabled for dev performance
// Import directly from sub-modules: import { apiRequest } from '@/shared/api'
// Re-enable when ready to migrate imports to FSD paths
