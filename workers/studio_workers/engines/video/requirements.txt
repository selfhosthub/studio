# Video Worker Dependencies (shs-video provider)
# Extends base requirements.txt

# Speech-to-text on CTranslate2; no PyTorch in the tree
faster-whisper>=1.0.0

# Note: FFmpeg is installed via apt in Dockerfile.video
